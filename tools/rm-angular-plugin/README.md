# rm-angular-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rm-angular-plugin` to build the library.

## Running unit tests

Run `nx test rm-angular-plugin` to execute the unit tests via [Jest](https://jestjs.io).
